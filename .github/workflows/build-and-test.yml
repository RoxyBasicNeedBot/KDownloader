name: Build & Test

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_call:

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─── Kotlin Multiplatform (Android + Desktop JVM) ───
  build-kmp:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'
      - uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/main' }}
      - name: Build Core + Android + Desktop
        run: ./gradlew build --no-daemon

      - name: Upload Android AAR Artifacts
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: kdownloader-android-aars
          path: |
            kdownloader-android/build/outputs/aar/*.aar
            kdownloader-compose/build/outputs/aar/*.aar
            kdownloader-hilt/build/outputs/aar/*.aar

  # ─── C# .NET Build ───
  build-dotnet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'
      - name: Link Linux Native Shared Binary
        run: |
          ./gradlew :kdownloader-dotnet-native:linkReleaseSharedNativeLinux
        continue-on-error: true

  # ─── Flutter Plugin ───
  build-flutter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
      - name: Flutter Pub Get
        run: |
          if [ -d "kdownloader-flutter" ]; then
            cd kdownloader-flutter && flutter pub get
          fi
        shell: bash
